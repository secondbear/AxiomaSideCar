[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pycdms-stub"
version = "0.0.1"
description = "Minimal stub of pycdms for CI — no real DICOM parsing"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
