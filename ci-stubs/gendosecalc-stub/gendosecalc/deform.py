# gendosecalc.deform CI stub


class DeformationConfig:
    n_states: int = 5


def generate_ensemble(ct_dir, motion_path, out_dir, config=None, **kwargs):
    """Stub — replaced by monkeypatch in tests."""
    raise NotImplementedError("CI stub")
