# gendosecalc.plan.clinical_run CI stub


class ClinicalRunContext:
    @classmethod
    def build(cls, rtplan_path, ct_dir, target_spacing_mm=2.5):
        """Stub — replaced by monkeypatch in tests."""
        raise NotImplementedError("CI stub")
