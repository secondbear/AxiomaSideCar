[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gendosecalc-stub"
version = "0.0.1"
description = "Minimal stub of gendosecalc for CI — no GPU, no patient data"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
